# Step_semester_3

Semester 3 coursework, organized per the STEP GitHub Workflow standard.

**Branches**
- `main` — this README, kept as a dated daily progress log (no code)
- `develop` — empty base Java project skeleton (no code)
- `feature/session_1` — topic `arrays_and_strings` (Week 1 assignment problems)
- `feature/session_4` — topic `arrays` (Week 4 Category C)
- `feature/session_5` — topic `arrays_and_methods` (Week 5 Category C)

Each feature branch is created from `develop` and holds one topic package with
`class_problems/` and `assigment_problems/` sub-packages.

---

## Date: 15-09-2026

**Today's Work:**
- Session 5 (`arrays_and_methods`): solved all 5 Category C problems — score curve booster (in-place array mutation), duplicate team finder, top-3 podium finder (single pass), seating grid optimizer (helper method), and the placement drive shortlisting & ranking engine (method overloading + `Comparable` + `Arrays.sort`).

**Next Session Plan:**
- Continue with the next topic's session branch.

**Issues Faced:**
- None.

---

## Date: DD-MM-YYYY   <!-- Session 4 — set this to the real session date -->

**Today's Work:**
- Session 4 (`arrays`): solved all 5 Category C problems — product of array except self, maximum subarray (Kadane's), 3Sum, subarray sum equals K (prefix sums + hash map), find minimum in rotated sorted array (binary search).

**Next Session Plan:**
- Arrays and methods session.

**Issues Faced:**
- None.

---

## Date: DD-MM-YYYY   <!-- Session 1 — set this to the real session date -->

**Today's Work:**
- Session 1 (`arrays_and_strings`): solved all 5 Week 1 problems — exam hall seat duplication checker, typing speed accuracy checker, traffic signal streak analyzer, warehouse inventory balancer, movie review word-length profiler.

**Next Session Plan:**
- Category C array problems.

**Issues Faced:**
- None.
